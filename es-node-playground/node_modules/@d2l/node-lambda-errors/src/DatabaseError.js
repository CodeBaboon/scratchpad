'use strict';

const ServerError = require('./ServerError');

class DatabaseError extends ServerError {
  constructor(cause, message, name) {
    super({
      name: name || 'DatabaseError',
      message: message || 'database error',
      cause
    });
  }
}

module.exports = DatabaseError;
