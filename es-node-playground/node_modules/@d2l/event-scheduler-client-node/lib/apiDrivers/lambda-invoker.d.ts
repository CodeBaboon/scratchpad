import { Event, STAGE } from '../event-scheduler-client';
export interface InvocationOptions {
    requestId: string;
}
export interface ConstructionOptions {
    aws: {
        accountId: string;
        region: string;
    };
    stage: STAGE;
}
export declare class LambdaDriver {
    private _options;
    constructor(options: ConstructionOptions);
    invokeServiceFunction(functionName: string, payload: Record<string, unknown>): Promise<Event>;
}
