import type { JsonObject } from '../@types';
declare const HTTPMethods: {
    readonly GET: "GET";
    readonly POST: "POST";
    readonly PUT: "PUT";
    readonly DELETE: "DELETE";
};
declare type HTTPMethods = typeof HTTPMethods[keyof typeof HTTPMethods];
/**
 * An HTTP client that automatically handles AWS-specific considerations, including credentials and request signing.
 */
export declare class AwsHttpDriver {
    private host;
    private roleArn;
    constructor(host: string, roleArn: string);
    request(method: HTTPMethods, uri: string, body?: JsonObject): Promise<JsonObject>;
}
export {};
